export async function connectGoogleCalendar() {
  // Placeholder para fluxo OAuth com Google Calendar API.
  return { connected: true, message: 'Integração Google Calendar pronta para configuração OAuth.' };
}

export async function syncBidirectional() {
  return { synced: true, message: 'Sincronização bidirecional opcional executada (mock).' };
}